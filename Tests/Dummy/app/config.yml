imports:
    - { resource: @FludioDoctrineEntityFactoryBundle/Resources/config/services.yml }

framework:
    secret: "DirtySecret"

doctrine:
    dbal:
        driver:   pdo_sqlite
        charset:  UTF8
        memory: true

    orm:
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true
